# Archive

Legacy trial scripts and ops helpers kept for reference.

- `legacy/` — superseded pipeline variants (`_v2`, Ks experiments, etc.)
- `ops/` — monitors / background helpers

Canonical entrypoints live in `../scripts/` (numbered `10_`–`94_`). Prefer `../../run_all.sh`.
